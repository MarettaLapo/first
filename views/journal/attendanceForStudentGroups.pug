extends ../head
block content
    ul(class="list-group mt-3")
        each el in studentGroups
            li(class="list-group-item")
                // ссылка на страницу добавления отметки посещаемости
                a(href="/addAttendanceMark/" + el.id) #{el.name}