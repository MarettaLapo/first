extends ../head
block content
    a(href="/addTeacher" class="btn btn-success") Добавить преподователя
    ul(class="list-group mt-3")
    each el in teachers
        li(class="list-group-item")
            a(href="/teacher/" + el.id) #{el.name}

            