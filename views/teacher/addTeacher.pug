extends ../head
block content
    form(action="/addTeacher" method="POST")
        input(name="name" type="text" placeholder="ФИО преподавателя" required)
        input(type="submit" value="Добавить")